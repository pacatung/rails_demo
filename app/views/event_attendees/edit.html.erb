<h2>Edit Attendee Name</h2>
<%= form_for @attendee, :url => event_attendee_path(@event, @attendee), :method=>:patch do |f| %>

  <%= f.label :name, 'Name' %>
  <%= f.text_field :name %>
  <%= f.submit %>

<% end %>